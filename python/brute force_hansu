def hanNum(n):
    cnt = 0
    for i in range(100,n+1):
        if int(str(i)[0]) - int(str(i)[1]) == int(str(i)[1]) - int(str(i)[2]):
            cnt +=1
    return(99+cnt)

n=int(input())

print(n if n < 100 else hanNum(n))